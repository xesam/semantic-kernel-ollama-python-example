## a example of run semantic-kernel with ollama[python]

```shell
conda create -n semantic-kernel python=3.12.0
activate semantic-kernel
pip install --upgrade semantic-kernel[all] #install semantic-kernel
python ./sk.python.py
```
